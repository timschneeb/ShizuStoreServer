# Icon rendering shares a small 2 vCPU host with the API; a single worker and
# no parallel project execution keep the Gradle daemon footprint bounded.
org.gradle.workers.max=1
org.gradle.parallel=false
android.useAndroidX=true
